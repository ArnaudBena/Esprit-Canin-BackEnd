# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: rename .example.env to .env
      run: mv .example.env .env

    - name: change localhost to db service name (postgres) in application.properties
      run: sed -i 's/ ${DB_HOST} /postgres/' src/main/resources/application.properties
      
    - name: Build with Maven
      run: mvn -B package --file pom.xml

    - name: Extract project version
      run: echo "VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_ENV

    - name: Build Docker Image
      run: docker build -t arnaudbena/esprit-canin-backend .

    - name: Tag image with version from pom.xml
      run: docker tag arnaudbena/esprit-canin-backend arnaudbena/esprit-canin-backend:${{ env.VERSION }}

    - name: Log in to Docker Hub
      uses: docker/login-action@v4
      with:
        username: arnaudbena
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Push Docker Image
      run: docker push arnaudbena/esprit-canin-backend:${{ env.VERSION }}